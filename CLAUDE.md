# AutoDrama - 自动化短剧制作系统

## 项目上下文

这是一个自动化短剧制作流水线系统，实现从系列大纲到最终成片的完整流程。

**核心流程**: 系列大纲 → 角色场景设定 → 美术资产生成 → 剧集创作(剧本→分镜→视频→成片)

> 详细任务定义见 task.json

---

## 核心概念

### 短剧系列 (Series)
一部完整的短剧作品，包含多个剧集。在创作之前必须完成：
- 美术风格设定
- 世界观设定
- 角色设定（外貌、性格、背景）
- 场景设定
- 剧集规划

### 美术资产
AI生成的固定视觉资产，保证整部短剧视觉一致性：
- 角色三视图（正面/侧面/背面）
- 场景参考图（多张）

### 剧集 (Episode)
系列中的单集，创作流程：剧本 → 分镜 → 视频片段 → 成片

---

## 强制要求: Agent 工作流

每个新的 Agent 会话必须遵循此工作流：

### Step 1: 初始化环境

```bash
./init.sh
```

这将:
- 安装所有依赖
- 启动开发服务器 http://localhost:3000

**不要跳过此步骤。** 在继续之前确保服务器正在运行。

### Step 2: 选择下一个任务

读取 `task.json` 并选择一个任务。

选择标准（按优先级）:
1. 选择 `passes: false` 的任务
2. 考虑依赖关系 - 基础功能优先
3. 选择优先级最高的未完成任务

### Step 3: 实现任务

- 仔细阅读任务描述和步骤
- 实现功能以满足所有步骤
- 遵循现有代码模式和约定

### Step 4: 彻底测试

实现后，验证任务中的所有步骤：

**强制测试要求:**

1. **大幅度页面修改**（新建页面、重写组件、修改核心交互）:
   - **必须在浏览器中测试！** 使用 MCP Playwright 工具
   - 验证页面正确加载和渲染
   - 验证表单提交、按钮点击等交互
   - 截图确认 UI 正确显示

2. **小幅度代码修改**（修复 bug、调整样式、添加辅助函数）:
   - 可以使用单元测试或 lint/build 验证
   - 如有疑虑，仍建议浏览器测试

3. **所有修改必须通过**:
   - `npm run lint` 无错误
   - `npm run build` 构建成功
   - 浏览器/单元测试验证功能正常

**测试清单:**
- [ ] 代码没有 TypeScript 错误
- [ ] lint 通过
- [ ] build 成功
- [ ] 功能在浏览器中正常工作（对于 UI 相关修改）

### Step 5: 更新进度

将工作记录到 `progress.txt`:

```
## [日期] - Task: [任务描述]

### 完成内容:
- [具体修改]

### 测试:
- [如何测试的]

### 备注:
- [给未来 Agent 的相关备注]
```

### Step 6: 提交更改（包含 task.json 更新）

**重要: 所有更改必须在同一个 commit 中提交，包括 task.json 的更新！**

流程:
1. 更新 `task.json`，将任务的 `passes` 从 `false` 改为 `true`
2. 更新 `progress.txt` 记录工作内容
3. 一次性提交所有更改:

```bash
git add .
git commit -m "[任务描述] - completed"
git push
```

**规则:**
- 只有在所有步骤都验证通过后才标记 `passes: true`
- 永远不要删除或修改任务描述
- 永远不要从列表中移除任务
- **一个 task 的所有内容（代码、progress.txt、task.json）必须在同一个 commit 中提交**

---

## ⚠️ 阻塞处理

**如果任务无法完成测试或需要人工介入，必须遵循以下规则：**

### 需要停止任务并请求人工帮助的情况：

1. **缺少环境配置**:
   - .env.local 需要填写真实的 API 密钥
   - Supabase 项目需要创建和配置
   - 外部服务需要开通账号

2. **外部依赖不可用**:
   - 第三方 API 服务宕机
   - 需要人工授权的 OAuth 流程
   - 需要付费升级的服务

3. **测试无法进行**:
   - 登录/注册功能需要真实用户账号
   - 功能依赖外部系统尚未部署
   - 需要特定硬件环境

### 阻塞时的正确操作：

**禁止:**
- ❌ 提交 git commit
- ❌ 将 task.json 的 passes 设为 true
- ❌ 假装任务已完成

**必须:**
- ✅ 在 progress.txt 中记录当前进度和阻塞原因
- ✅ 输出清晰的阻塞信息，说明需要人工做什么
- ✅ 停止任务，等待人工介入

### 阻塞信息格式：

```
🚫 任务阻塞 - 需要人工介入

**当前任务**: [任务名称]

**已完成的工作**:
- [已完成的代码/配置]

**阻塞原因**:
- [具体说明为什么无法继续]

**需要人工帮助**:
1. [具体的步骤 1]
2. [具体的步骤 2]
...

**解除阻塞后**:
- 运行 [命令] 继续任务
```

---

## 项目结构

```
/
├── CLAUDE.md          # 本文件 - 工作流指令
├── task.json          # 任务定义（事实来源）
├── progress.txt       # 每次会话的进度记录
├── task_plan.md       # 任务规划
├── findings.md        # 探索发现
├── init.sh            # 初始化脚本
├── architecture.md    # 架构设计
├── autodrama/         # Next.js 应用（待创建）
│   ├── src/app/       # App Router 页面
│   ├── src/components/
│   └── ...
└── auto-coding-agent-demo/  # 参考项目（不要修改）
```

## 命令

```bash
# 在 autodrama/
npm run dev      # 启动开发服务器
npm run build    # 生产构建
npm run lint     # 运行 linter
```

## 编码约定

- TypeScript strict mode
- 函数式组件 + hooks
- Tailwind CSS 样式
- 为新功能编写测试

---

## 关键规则

1. **每个会话一个任务** - 专注完成一个任务
2. **标记完成前先测试** - 所有步骤必须通过
3. **UI 修改需要浏览器测试** - 新建或大幅修改页面必须在浏览器测试
4. **在 progress.txt 中记录** - 帮助未来的 Agent 理解你的工作
5. **每个任务一个 commit** - 所有更改（代码、progress.txt、task.json）必须在同一个 commit 中提交
6. **不要移除任务** - 只将 `passes: false` 改为 `true`
7. **阻塞时停止** - 需要人工介入时，不要提交，输出阻塞信息并停止
8. **不要修改 auto-coding-agent-demo/** - 仅作为参考